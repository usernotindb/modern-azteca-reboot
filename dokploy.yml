
version: '1'
name: aztecas-website
type: web
env:
  - NODE_ENV=production
buildCommand: rm -rf node_modules bun.lockb && bun install && bun run build
startCommand: node server.js
port: 3235
resources:
  cpu: 0.5
  memory: 512Mi
  replicas: 2
